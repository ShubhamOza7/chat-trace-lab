# Core model access
anthropic>=0.69.0

# LangChain path (CHAT_BACKEND=langchain)
langchain-core>=0.3.0
langchain-anthropic>=0.3.0

# Tracing — vendor-neutral OpenTelemetry
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0

# HTTP server + web UI
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Config
python-dotenv>=1.0.0
